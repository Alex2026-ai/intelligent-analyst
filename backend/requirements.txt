# Intelligent Analyst v8.2.2 Enterprise
# Requirements

# Core
pandas>=2.0.0
numpy>=1.24.0

# Layer 2: Vector Similarity
scikit-learn>=1.3.0

# Layer 2: Person Name Matching
jellyfish>=0.9.0     # Jaro-Winkler, Soundex, Metaphone for fuzzy name matching

# Layer 3: LLM (Optional - only if using Claude)
anthropic>=0.18.0

# FastAPI for serving
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Excel file support
openpyxl>=3.1.0      # Modern .xlsx files
xlrd>=2.0.0          # Legacy .xls files

# PDF Certificates
reportlab>=4.0.0     # Transparency certificates

# Google Cloud
google-cloud-firestore>=2.16.0  # Batch history & audit storage
firebase-admin>=6.0.0           # Firebase ID token verification
google-cloud-kms>=2.21.0        # KMS signing for forensic audit
google-cloud-storage>=2.14.0    # GCS for anchoring + WORM vaulting

# Cryptography (tenant envelope encryption)
cryptography>=41.0.0            # AES-256-GCM for evidence blob encryption

# Config
PyYAML>=6.0                     # Policy engine (assertion layer)

# Optional: Testing
pytest>=7.4.0
google-cloud-tasks>=2.14.0
